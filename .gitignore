# Terraform files
*.tfstate
*.tfstate.backup
*.tfplan
.terraform/

# tfvars files (sensitive environment-specific data)
terraform.tfvars
terraform.tfvars.json

# IDE settings
.vscode/

# Keep Terraform dependency lock file (safe and recommended to commit)
!.terraform.lock.hcl